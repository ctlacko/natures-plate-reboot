A new user is requesting access to NaturesPlate.biz!

Here is their information:

Username: <%= @user.email %>

Go to the URL below to confirm access. This will allow the user to place orders.

<%= link_to "Confirm access", confirm_url(:id => @user.id, :confirmation_code => @user.confirmation_code) %>

<%= confirm_url(:id => @user.id, :confirmation_code => @user.confirmation_code) %>

