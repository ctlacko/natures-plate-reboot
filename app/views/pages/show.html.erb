<div id="page-content" class="span-24 <%= @page.name %>">
  <h1 class="center"><%= @page.name %></h1>

  <%= simple_format @page.content %>

  <%= link_to 'Edit', edit_page_path(@page) %> |
  <%= link_to 'Back', pages_path %>
</div>
